#!/bin/sh

rm -rf target

echo Run with ant...

time -p ant jar > /dev/null

rm -rf target

echo Run with m2...

time -p m2 -B package > /dev/null

rm -rf target

echo Run with maven...

time -p maven jar > /dev/null
